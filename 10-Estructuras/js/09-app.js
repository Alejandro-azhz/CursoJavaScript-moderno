const auntenticado = true;
const puedePagar = true;

console.log(auntenticado ? puedePagar ? 'si esta autenticado y puede pagar' : 'Si autenticado, no puede pagar' : ' No  No esta autenticado ');









//if anidado//

/* const efectivo = 300;
const credito = 400;
const disponible = efectivo + credito;
const totalPagar = 600;

if(efectivo > totalPagar || credito < totalPagar || disponible > totalPagar ) {
    if(efectivo > totalPagar) {
        console.log('si pagaste con efectivo');
    } else {
        console.log('otra forma de pago');
    }   
} else {
    console.log('fondos insuficientes');
} */